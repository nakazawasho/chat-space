.message__name-date
  %ul
    %li.list-name
      =message.user.name
    %li.list-date
      =message.created_at.strftime('%Y/%m/%d %H:%M')
.message__text
  %p
    =message.content
  -if message.image_url(:thumb)
    = image_tag message.image_url(:thumb)
